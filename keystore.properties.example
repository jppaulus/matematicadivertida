# Copy this file to keystore.properties (same folder, project root) and fill in
# your real values. keystore.properties is gitignored and must never be committed.

storeFile=matematica-divertida.jks
storePassword=changeme
keyAlias=matematica-divertida-key
keyPassword=changeme
